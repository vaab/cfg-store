links:
  ## How to link $CHARM/src/* in your current $HOME/*
  src:
    ## All keys start from $HOME, all values start from $CHARM_PATH/src
    alacritty.toml: .config/alacritty/
    host/$HOSTNAME.toml: .config/alacritty/local.toml
