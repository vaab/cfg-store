# -*- mode: shell-script -*-

export ANDROID_HOME=/opt/apps/android-sdk

path-prepend "$ANDROID_HOME/platform-tools"
path-prepend "$ANDROID_HOME/tools"

function emulator { cd "$(dirname "$(which emulator)")" && ./emulator "$@"; }
