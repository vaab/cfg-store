uses:
  ssh-client:
    constraint: required
    auto: pair
    solves:
      client: "ssh client"
    links:
      ## All keys start from ${CHARM_PATH}/relations/{relation_name}, all value start from $HOME
      10-ssh_agent: .ssh/config.d/
